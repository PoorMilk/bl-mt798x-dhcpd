> [!WARNING]
> 本项目 `main` 分支由 GitHub Actions 每日定时强制同步上游（`git push -f`），提交历史会被频繁重置。  
> 请勿使用 `git pull` 更新源码，本地更新请执行：
> ```bash
> git fetch origin && git reset --hard origin/main
> ```
> *如需修改代码，请切出新分支开发，切勿直接在 `main` 分支上提交。*
